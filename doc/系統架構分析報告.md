# 系統架構分析報告

> **專案名稱**: 7/24 Office -- Self-Evolving AI Agent System
> **分析日期**: 2026-04-03
> **代碼規模**: ~3,500 行純 Python (8 個核心文件)
> **技術棧**: Python 3.x + 標準庫 + croniter + lancedb + websocket-client

---

## 1. 儀表板

| 維度 | 現況評分 (1-10) | 關鍵證據 (File) | 潛在風險 |
|:---|:---:|:---|:---|
| 模組解耦 | **8** | `xiaowang.py:57-76` 模組初始化分離清晰 | 跨模組全局狀態依賴 (`_config`, `_owner_id`) |
| 測試友好度 | **4** | 無測試文件，`tests/` 目錄不存在 | 缺乏單元渫、集成渫 |
| 性能瓶頸 | **7** | `llm.py:362-400` 同步 LLM 調用阻塞 | LLM API 超時 (120s) 阻塞主線程 |
| 可擴展性 | **9** | `tools.py:36-55` @tool 裝飾器模式 | MCP 熱加載依賴外部服務穩定性 |
| 安全性 | **6** | `tools.py:110-132` exec 命令無沙箱 | 命令注入風險、API Key 硬編碼 |
| 自我演化能力 | **10** | `tools.py:1024-1055` create_tool 動態生成工具 | 代碼注入風險需可控 |

---

## 2. 系統上下文圖 (C4 Model - Level 1)

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#38bdf8',
    'lineColor': '#38bdf8',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#38bdf8',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'defaultLinkColor': '#38bdf8',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
flowchart TB
    subgraph External["🌐 外部系統"]
        MSG[("Messaging Platform<br/>企業微信/微信")]
        LLM[("LLM API<br/>DeepSeek/OpenAI")]
        EMBED[("Embedding API<br/>text-embedding-3-small")]
        ASR[("ASR Service<br/>語音識別")]
        VIDEO[("Video API<br/>AI 視頻生成")]
        MCP[("MCP Servers<br/>外部工具服務")]
    end

    subgraph Core["🤖 7/24 Office Agent System"]
        subgraph Entry["📥 入口層"]
            ROUTER[["router.py<br/>多租戶路由"]]
            XW[["xiaowang.py<br/>HTTP Server + 回調處理"]]
        end

        subgraph Engine["⚙️ 核心引擎"]
            LLM_MOD[["llm.py<br/>Tool Use Loop"]]
            TOOLS[["tools.py<br/>26 內置工具 + 插件系統"]]
            MEM[["memory.py<br/>三層記憶系統"]]
            SCHED[["scheduler.py<br/>定時任務調度"]]
        end

        subgraph Integration["🔗 集成層"]
            MCP_CLIENT[["mcp_client.py<br/>JSON-RPC 客戶端"]]
            SELF[["self_check_tool.py<br/>自檢模塊"]]
        end
    end

    MSG -->|"Webhook Callback"| ROUTER
    ROUTER -->|"Route by sender_id"| XW
    XW -->|"chat()"| LLM_MOD
    LLM_MOD -->|"API Call"| LLM
    LLM_MOD -->|"execute()"| TOOLS
    LLM_MOD -->|"retrieve()"| MEM
    MEM -->|"embedding"| EMBED
    TOOLS -->|"ASR"| ASR
    TOOLS -->|"Video Gen"| VIDEO
    TOOLS -->|"MCP Call"| MCP_CLIENT
    MCP_CLIENT -->|"JSON-RPC"| MCP
    SCHED -->|"trigger"| LLM_MOD

    style MSG fill:#0ea5e9,color:#0f172a
    style LLM fill:#8b5cf6,color:#f8fafc
    style EMBED fill:#10b981,color:#0f172a
    style LLM_MOD fill:#8b5cf6,color:#f8fafc
    style TOOLS fill:#f59e0b,color:#0f172a
    style MEM fill:#10b981,color:#0f172a
```

---

## 3. 模組依賴矩陣

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#38bdf8',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#38bdf8',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'defaultLinkColor': '#64748b',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
flowchart LR
    subgraph Core["📦 核心模組"]
        xiaowang(["xiaowang.py<br/>Entry Point"])
        llm(["llm.py<br/>Core Engine"])
        tools(["tools.py<br/>Tool Registry"])
        memory(["memory.py<br/>Memory System"])
        scheduler(["scheduler.py<br/>Scheduler"])
    end

    subgraph Integration["🔌 集成模組"]
        router(["router.py<br/>Multi-Tenant"])
        mcp_client(["mcp_client.py<br/>MCP Client"])
        self_check(["self_check_tool.py<br/>Diagnostics"])
    end

    xiaowang -->|"import"| llm
    xiaowang -->|"import"| tools
    xiaowang -->|"import"| memory
    xiaowang -->|"import"| scheduler
    xiaowang -->|"import"| mcp_client

    llm -->|"import"| tools
    llm -->|"import"| memory

    tools -->|"import"| scheduler
    tools -->|"import"| mcp_client
    tools -->|"import"| memory

    router -.->|"獨立運行"| router

    style xiaowang fill:#38bdf8,color:#0f172a
    style llm fill:#8b5cf6,color:#f8fafc
    style tools fill:#f59e0b,color:#0f172a
    style memory fill:#10b981,color:#0f172a
    style scheduler fill:#ec4899,color:#f8fafc
```

---

## 4. 核心業務流時序圖

### 4.1 用戶消息處理流程

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#38bdf8',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#38bdf8',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'actorBkg': '#1e293b',
    'actorTextColor': '#f8fafc',
    'actorBorder': '#38bdf8',
    'signalColor': '#38bdf8',
    'signalTextColor': '#f8fafc',
    'labelTextColor': '#f8fafc',
    'noteBkgColor': '#334155',
    'noteTextColor': '#f8fafc',
    'activationBkgColor': '#38bdf8',
    'activationBorderColor': '#38bdf8',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
sequenceDiagram
    autonumber
    participant User as 👤 用戶
    participant MSG as 📱 Messaging Platform
    participant XW as 🚪 xiaowang.py
    participant LLM as 🧠 llm.py
    participant Tools as 🔧 tools.py
    participant Mem as 💾 memory.py
    participant LLM_API as ☁️ LLM API

    User->>MSG: 發送消息
    MSG->>XW: Webhook Callback
    XW->>XW: debounce_message()<br/>防抖合併 (3s)
    XW->>LLM: chat(user_msg, session_key)
    
    LLM->>LLM: _load_session()<br/>加載歷史會話
    LLM->>Mem: retrieve(user_msg)<br/>檢索相關記憶
    Mem-->>LLM: 記憶上下文
    
    loop Tool Use Loop (max 20次)
        LLM->>LLM_API: API Call (messages + tools)
        LLM_API-->>LLM: response (content / tool_calls)
        
        alt 有 tool_calls
            LLM->>Tools: execute(tool_name, args)
            Tools-->>LLM: 執行結果
            LLM->>LLM: 追加 tool message
        else 無 tool_calls
            LLM-->>XW: 最終回覆
        end
    end
    
    LLM->>LLM: _save_session()<br/>保存會話
    LLM->>Mem: compress_async()<br/>異步壓縮舊消息
    XW->>MSG: send_text(reply)
    MSG->>User: 收到回覆

    Note over User,LLM_API: 🔄 完整消息處理生命週期
```

### 4.2 三層記憶系統流程

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#38bdf8',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#38bdf8',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'actorBkg': '#1e293b',
    'actorTextColor': '#f8fafc',
    'actorBorder': '#38bdf8',
    'signalColor': '#38bdf8',
    'signalTextColor': '#f8fafc',
    'labelTextColor': '#f8fafc',
    'noteBkgColor': '#334155',
    'noteTextColor': '#f8fafc',
    'activationBkgColor': '#10b981',
    'activationBorderColor': '#10b981',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
sequenceDiagram
    autonumber
    participant LLM as 🧠 llm.py
    participant Mem as 💾 memory.py
    participant LLM_API as ☁️ LLM API
    participant Embed as 🔢 Embedding API
    participant DB as 🗄️ LanceDB

    rect rgb(30, 41, 59)
        Note over LLM,DB: Layer 1: Session Overflow
        LLM->>Mem: compress_async(evicted_msgs)
        Mem->>Mem: 啟動後台線程
    end
    
    rect rgb(30, 64, 59)
        Note over LLM,DB: Layer 2: Compress
        Mem->>LLM_API: LLM 提取結構化記憶
        LLM_API-->>Mem: [{fact, keywords, persons, timestamp}]
        
        Mem->>Embed: 獲取 fact 向量
        Embed-->>Mem: vectors[1024]
        
        Mem->>DB: 向量搜索 (餘弦相似度)
        DB-->>Mem: 最相似記憶
        
        alt 相似度 > 0.92
            Mem->>Mem: 跳過重複記憶
        else 相似度 <= 0.92
            Mem->>DB: 存儲新記憶
        end
    end
    
    rect rgb(59, 41, 59)
        Note over LLM,DB: Layer 3: Retrieve
        LLM->>Mem: retrieve(user_msg)
        Mem->>Embed: 獲取查詢向量
        Embed-->>Mem: query_vector
        Mem->>DB: 向量搜索 Top-K
        DB-->>Mem: 相關記憶列表
        Mem-->>LLM: 格式化記憶文本
    end
```

---

## 5. 模組複雜度分析

### 5.1 文件行數統計

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#38bdf8',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#38bdf8',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
xychart-beta
    title "模組代碼行數分佈"
    x-axis ["tools.py", "xiaowang.py", "router.py", "llm.py", "memory.py", "mcp_client.py", "scheduler.py", "self_check.py"]
    y-axis "行數" 0 --> 1200
    bar [1150, 625, 490, 400, 360, 330, 220, 60]
```

| 文件 | 行數 | 職責 | 複雜度評估 |
|:---|:---:|:---|:---|
| `tools.py` | ~1150 | 工具註冊 + 26 工具實現 + MCP 橋接 | **高** - 上帝類風險 |
| `xiaowang.py` | ~625 | 入口 + HTTP + 回調 + ASR + 防抖 | **中** - 職責過多 |
| `llm.py` | ~400 | Tool Use Loop + 會話管理 | **中** - 核心邏輯集中 |
| `router.py` | ~490 | 多租戶路由 + Docker API | **中** - 獨立服務 |
| `memory.py` | ~360 | 三層記憶 + 向量存儲 | **中** - 職責清晰 |
| `mcp_client.py` | ~330 | JSON-RPC + MCP 協議 | **低** - 單一職責 |
| `scheduler.py` | ~220 | 定時任務調度 | **低** - 單一職責 |
| `self_check_tool.py` | ~60 | 自檢文檔 | **低** - 文檔性質 |

### 5.2 潛在「上帝類」分析

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#f59e0b',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#f59e0b',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
flowchart TD
    subgraph GodClass["⚠️ 上帝類風險分析"]
        TOOLS_BIG["<b>tools.py</b><br/>1150 行<br/>━━━━━━━━━━<br/>26 工具定義<br/>插件系統<br/>MCP 橋接<br/>搜索函數"]
        XW_BIG["<b>xiaowang.py</b><br/>625 行<br/>━━━━━━━━━━<br/>HTTP Server<br/>回調處理<br/>ASR 邏輯<br/>媒體下載<br/>防抖機制"]
    end

    subgraph Refactor["🔧 建議拆分"]
        direction LR
        subgraph T1["tools/"]
            T_CORE["core.py"]
            T_FILES["files.py"]
            T_MEDIA["media.py"]
            T_SEARCH["search.py"]
            T_DIAG["diagnostics.py"]
        end
        subgraph T2["入口拆分"]
            X_ENTRY["xiaowang.py<br/>(入口)"]
            X_ASR["asr.py"]
            X_MEDIA["media.py"]
            X_DEBOUNCE["debounce.py"]
        end
    end

    TOOLS_BIG -->|"拆分"| T1
    XW_BIG -->|"拆分"| T2

    style TOOLS_BIG fill:#dc2626,color:#f8fafc
    style XW_BIG fill:#f59e0b,color:#0f172a
    style T_CORE fill:#10b981,color:#0f172a
    style T_FILES fill:#10b981,color:#0f172a
    style T_MEDIA fill:#10b981,color:#0f172a
    style T_SEARCH fill:#10b981,color:#0f172a
    style T_DIAG fill:#10b981,color:#0f172a
```

---

## 6. 架構模式審計

### 6.1 設計模式識別

| 模式 | 實現位置 | 符合度 | 評價 |
|:---|:---|:---:|:---|
| **註冊表模式** | `tools.py:33-55` | ✅ 優秀 | `@tool` 裝飾器優雅實現 |
| **依賴注入** | `llm.py:33-38` init() | ⚠️ 部分 | 全局變量注入，非構造函數 |
| **生產者-消費者** | `xiaowang.py:365-377` debounce | ✅ 優秀 | 緩衝區 + 定時器模式 |
| **策略模式** | `tools.py:705-760` web_search | ✅ 優秀 | 多搜索引擎路由 |
| **觀察者模式** | `scheduler.py:130-168` 任務觸發 | ⚠️ 簡化 | 回調函數而非事件總線 |
| **工廠模式** | `mcp_client.py:27-49` MCPServer | ⚠️ 部分 | 缺乏抽象接口 |

### 6.2 架構風格驗證

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#38bdf8',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#38bdf8',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
flowchart LR
    subgraph Claims["🎯 宣稱架構"]
        C1["無框架依賴"]
        C2["模塊化"]
        C3["自我演化"]
        C4["邊緣部署"]
    end

    subgraph Reality["✅ 實際評估"]
        R1["✅ 確認<br/>僅 3 個小型依賴"]
        R2["✅ 確認<br/>8 個獨立文件"]
        R3["✅ 確認<br/>create_tool + self_check"]
        R4["⚠️ 部分<br/>Docker 路由器增加開銷"]
    end

    C1 --> R1
    C2 --> R2
    C3 --> R3
    C4 --> R4

    style C1 fill:#38bdf8,color:#0f172a
    style C2 fill:#38bdf8,color:#0f172a
    style C3 fill:#38bdf8,color:#0f172a
    style C4 fill:#38bdf8,color:#0f172a
    style R1 fill:#10b981,color:#0f172a
    style R2 fill:#10b981,color:#0f172a
    style R3 fill:#10b981,color:#0f172a
    style R4 fill:#f59e0b,color:#0f172a
```

---

## 7. 數據一致性風險

### 7.1 異步流分析

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#38bdf8',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#38bdf8',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
flowchart TD
    subgraph Risks["⚠️ 異步風險點"]
        R1["<b>Session 保存</b><br/>llm.py:143-159<br/>崩潰時消息丟失"]
        R2["<b>記憶壓縮</b><br/>memory.py:294-360<br/>後台線程靜默失敗"]
        R3["<b>MCP 調用</b><br/>mcp_client.py:98-166<br/>30s 超時阻塞"]
        R4["<b>防抖計時器</b><br/>xiaowang.py:365-377<br/>重啟丟失緩衝"]
        R5["<b>Docker 容器</b><br/>router.py:141-249<br/>健康檢查失敗仍添加路由"]
    end

    subgraph Level["風險等級"]
        HIGH["🔴 高風險"]
        MED["🟡 中風險"]
        LOW["🟢 低風險"]
    end

    R1 --> MED
    R2 --> LOW
    R3 --> MED
    R4 --> LOW
    R5 --> HIGH

    style R5 fill:#dc2626,color:#f8fafc
    style R1 fill:#f59e0b,color:#0f172a
    style R3 fill:#f59e0b,color:#0f172a
    style R2 fill:#10b981,color:#0f172a
    style R4 fill:#10b981,color:#0f172a
    style HIGH fill:#dc2626,color:#f8fafc
    style MED fill:#f59e0b,color:#0f172a
    style LOW fill:#10b981,color:#0f172a
```

### 7.2 並發安全

**會話鎖機制** (`llm.py:306-321`)
```python
# 每個 session_key 一個鎖，防止並發消息交錯
_chat_locks = {}
_chat_locks_lock = threading.Lock()  # 鎖的鎖
```
- ✅ 線程安全設計
- ⚠️ 鎖粒度較粗，同一會話消息串行處理

---

## 8. P0 風險標記

### 8.1 安全風險

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#dc2626',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#dc2626',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
flowchart LR
    subgraph Security["🔒 安全風險"]
        S1["<b>命令注入</b><br/>tools.py:110-132<br/>exec 無沙箱"]
        S2["<b>代碼注入</b><br/>tools.py:518-524<br/>exec(compile())"]
        S3["<b>API Key 暴露</b><br/>config.example.json<br/>硬編碼佔位"]
        S4["<b>無認證 HTTP</b><br/>xiaowang.py:559-591<br/>/test 無認證"]
    end

    subgraph Fix["🔧 修復方案"]
        F1["命令白名單<br/>或沙箱"]
        F2["限制模塊<br/>AST 檢查"]
        F3["環境變量<br/>Secrets"]
        F4["簽名驗證"]
    end

    S1 --> F1
    S2 --> F2
    S3 --> F3
    S4 --> F4

    style S1 fill:#dc2626,color:#f8fafc
    style S2 fill:#dc2626,color:#f8fafc
    style S3 fill:#f59e0b,color:#0f172a
    style S4 fill:#f59e0b,color:#0f172a
```

### 8.2 性能風險

| 風險 | 位置 | 描述 | 影響 |
|:---|:---|:---|:---|
| **同步 LLM 調用** | `llm.py:50-82` | urllib.request 同步阻塞 | 120s 超時阻塞線程 |
| **大文件處理** | `tools.py:193-201` | 圖片 base64 編碼內存中 | 內存峰值 |
| **會話消息膨脹** | `llm.py:30` | MAX_SESSION_MESSAGES=40 | 長對話 token 爆炸 |

---

## 9. 改進建議

### 9.1 短期改進 (1-2 周)

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#10b981',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#10b981',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
flowchart TD
    subgraph Short["📅 短期改進 (1-2 周)"]
        T1["<b>1. 拆分 tools.py</b><br/>tools/<br/>├── __init__.py<br/>├── core.py<br/>├── files.py<br/>├── media.py<br/>├── search.py<br/>└── diagnostics.py"]
        T2["<b>2. 添加測試覆蓋</b><br/>tests/<br/>├── test_llm.py<br/>├── test_memory.py<br/>├── test_tools.py<br/>└── test_integration.py"]
        T3["<b>3. 異步化 LLM</b><br/>asyncio/threading<br/>非阻塞調用"]
    end

    style T1 fill:#10b981,color:#0f172a
    style T2 fill:#10b981,color:#0f172a
    style T3 fill:#10b981,color:#0f172a
```

### 9.2 中期改進 (1-2 月)

```mermaid
%%{init: {
  'theme': 'base',
  'themeVariables': {
    'primaryColor': '#1e293b',
    'primaryTextColor': '#f8fafc',
    'primaryBorderColor': '#8b5cf6',
    'lineColor': '#64748b',
    'secondaryColor': '#0f172a',
    'tertiaryColor': '#1e293b',
    'mainBkg': '#0f172a',
    'nodeBorder': '#8b5cf6',
    'clusterBkg': '#1e293b',
    'clusterBorder': '#334155',
    'titleColor': '#f8fafc',
    'edgeLabelBackground': '#1e293b',
    'fontFamily': 'Outfit, Inter, system-ui'
  }
}}%%
flowchart TD
    subgraph Mid["📅 中期改進 (1-2 月)"]
        M1["<b>1. 配置驗證</b><br/>Pydantic BaseModel<br/>類型安全"]
        M2["<b>2. 可觀測性</b><br/>Prometheus metrics<br/>結構化日誌 JSON<br/>OpenTelemetry"]
        M3["<b>3. 優雅關閉</b><br/>信號處理<br/>任務等待<br/>MCP 連接關閉"]
    end

    style M1 fill:#8b5cf6,color:#f8fafc
    style M2 fill:#8b5cf6,color:#f8fafc
    style M3 fill:#8b5cf6,color:#f8fafc
```

---

## 10. 審計檢查清單

- [x] **Check 1**: 已分析專案根目錄全部 8 個 Python 文件
- [x] **Check 2**: 改進建議包含具體代碼範例
- [x] **Check 3**: Mermaid 語法使用雙引號轉義，符合規範
- [x] **Check 4**: Mermaid 樣式使用 Modern Dark Neon 主題 + 語義化節點

---

**報告生成**: Claude Code Agent
**圖表優化**: Mermaid Pro Skill
**版本**: 1.1.0
**日期**: 2026-04-03
