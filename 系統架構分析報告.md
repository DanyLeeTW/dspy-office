# 系統架構分析報告 — dspy-office

> **生成日期**：2026-04-03  
> **分析範圍**：全專案代碼庫（排除 node_modules / dist / .git）  
> **分析師**：AI Architecture Analyst (Claude Sonnet 4.6)

---

## 一、架構儀表板

| 維度 | 現況評分 (1-10) | 關鍵證據 (File) | 潛在風險 |
| :--- | :---: | :--- | :--- |
| 模組解耦 | 6 | `dspy_agent/__init__.py` 609 行，混合 orchestration + state | Agent 類承擔過多職責（God Class 傾向） |
| 測試友好度 | 4 | `test_dspy_setup.py` 為唯一測試入口；無 mock 策略 | 無法獨立測試 LLM / Tool / Memory 各層 |
| 性能瓶頸 | 5 | `llm.py` 工具循環為同步阻塞；`ThreadingMixIn` 規避但不解決 | 高並發下線程爆炸；無連接池 |
| 配置安全性 | 3 | `config.json` 明文存 API Key（Tencent、Gemini、WeChat） | 憑證洩漏風險極高 |
| 擴展性 | 7 | `@tool` 裝飾器插拔工具；DSPy Teleprompter 可優化 | 多租戶靠 Docker 路由，未內化至 Framework |
| 記憶體管理 | 8 | LanceDB 向量庫 + 三階段壓縮/去重/檢索 | 相似度閾值 0.92 固定，無動態調整機制 |

---

## 二、C4 系統上下文圖（Level 1）

```mermaid
%%{init: {
  'theme': 'dark',
  'themeVariables': {
    'primaryColor': '#464040f8',
    'primaryTextColor': '#ffffff',
    'primaryBorderColor': '#ffffff',
    'lineColor': '#ffffff',
    'secondaryColor': '#ffffff',
    'tertiaryColor': '#ffffff',
    'noteBkgColor': '#1e1e1e',
    'noteTextColor': '#ffffff',
    'actorBkg': '#1e1e1e',
    'actorTextColor': '#ffffff',
    'actorBorder': '#ffffff',
    'messageColor': '#ffffff',
    'messageTextColor': '#ffffff',
    'labelTextColor': '#ffffff',
    'loopTextColor': '#ffffff',
    'textColor': '#ffffff',
    'activationBkgColor': '#3a3a3a',
    'sequenceNumberColor': '#ffffff',
    'fontSize': '16px',
    'fontFamily': 'ui-sans-serif, system-ui, sans-serif'
  }
}}%%
C4Context
    title "dspy-office — 系統上下文圖 (C4 Level 1)"

    Person(user_wechat, "微信用戶", "透過企業微信與 AI Agent 互動")
    Person(user_web, "Web 用戶", "透過 React Dashboard 提交任務目標")

    System(dspy_office, "dspy-office AI Agent", "自進化 AI Agent 系統：\n接收指令 → 規劃步驟 → 呼叫工具 → 記憶壓縮 → 回應")

    System_Ext(tencent_llm, "Tencent Hunyuan (glm-5)", "主要推理模型\napi.lkeap.cloud.tencent.com")
    System_Ext(gemini_embed, "Google Gemini Embedding", "記憶向量化\ngenerativelanguage.googleapis.com")
    System_Ext(wechat_work, "企業微信 API", "訊息收發、媒體下載")
    System_Ext(docker_engine, "Docker Engine", "多租戶容器路由")

    Rel(user_wechat, dspy_office, "發送訊息 / 語音 / 圖片", "HTTPS Callback")
    Rel(user_web, dspy_office, "提交 Agent Goal", "HTTP POST /api/chat")
    Rel(dspy_office, tencent_llm, "推理請求 / 工具決策", "OpenAI-compatible API")
    Rel(dspy_office, gemini_embed, "嵌入記憶向量", "REST API")
    Rel(dspy_office, wechat_work, "回覆訊息 / 下載媒體", "HTTPS")
    Rel(dspy_office, docker_engine, "路由多租戶容器", "Docker SDK")
```

---

## 三、容器架構圖（C4 Level 2）

```mermaid
%%{init: {
  'theme': 'dark',
  'themeVariables': {
    'primaryColor': '#464040f8',
    'primaryTextColor': '#ffffff',
    'primaryBorderColor': '#ffffff',
    'lineColor': '#ffffff',
    'secondaryColor': '#464040f8',
    'tertiaryColor': '#2a2a2a',
    'noteBkgColor': '#1e1e1e',
    'noteTextColor': '#ffffff',
    'textColor': '#ffffff',
    'fontSize': '16px',
    'fontFamily': 'ui-sans-serif, system-ui, sans-serif'
  }
}}%%
C4Container
    title "dspy-office — 容器架構圖 (C4 Level 2)"

    Container(frontend, "React Dashboard", "Rsbuild + TypeScript", "視覺化 Agent 執行步驟與記憶狀態")
    Container(backend_main, "Python HTTP Server", "HTTPServer + ThreadingMixIn\n:8080", "核心業務邏輯：接收 → 推理 → 工具 → 記憶")
    Container(dspy_agent, "DSPy Agent Module", "dspy_agent/__init__.py\n609 行", "Agent 編排、Teleprompter 優化接口")
    Container(router, "Multi-tenant Router", "router.py", "依 sender_id 路由至對應 Docker 容器")
    ContainerDb(lancedb, "LanceDB Vector Store", "memory_db/memories.lance", "向量記憶持久化")
    ContainerDb(sessions, "Session Store", "sessions/*.json", "對話歷史，按 session_key 存儲")

    Rel(frontend, backend_main, "POST /api/chat", "JSON over HTTP")
    Rel(backend_main, dspy_agent, "呼叫 Agent.forward()", "Python in-process")
    Rel(backend_main, lancedb, "讀寫記憶向量", "LanceDB Python API")
    Rel(backend_main, sessions, "載入/儲存對話", "File I/O (JSON)")
    Rel(router, backend_main, "代理請求", "HTTP Proxy")
```

---

## 四、模組依賴矩陣（Mermaid Graph）

```mermaid
%%{init: {
  'theme': 'dark',
  'themeVariables': {
    'primaryColor': '#464040f8',
    'primaryTextColor': '#ffffff',
    'primaryBorderColor': '#ffffff',
    'lineColor': '#aaaaaa',
    'secondaryColor': '#2a2a2a',
    'tertiaryColor': '#3a3a3a',
    'textColor': '#ffffff',
    'fontSize': '16px',
    'fontFamily': 'ui-sans-serif, system-ui, sans-serif'
  }
}}%%
graph TD
    subgraph "Entry Points"
        XW["xiaowang.py\n原始入口"]
        DX["dspy_xiaowang.py\nDSPy 入口"]
        RT["router.py\n多租戶路由"]
    end

    subgraph "Core Layer"
        LLM["llm.py\nLLM 呼叫 + 工具循環"]
        DA["dspy_agent/\nAgent 編排模組"]
        SIG["signatures/__init__.py\nDSPy 簽章定義"]
        MOD["modules/__init__.py\nAgent / ToolAgent / MemoryModule"]
    end

    subgraph "Services"
        MEM["memory.py\n三階段記憶管線"]
        TOOLS["tools.py\n工具登錄表"]
        SCH["scheduler.py\nAPScheduler 排程"]
        MSG["messaging.py\n企業微信 API"]
        MCP["mcp_client.py\nMCP 伺服器整合"]
    end

    subgraph "Storage"
        LDB[("LanceDB\n向量記憶")]
        SES[("sessions/\n對話歷史 JSON")]
        WS[("workspace/\n用戶文件")]
    end

    subgraph "Frontend"
        APP["App.tsx\n儀表板主頁"]
        HOOK["useAgentRunner.ts\nAgent 執行 Hook"]
        SVC["llm.ts\nBackend API 客戶端"]
        COMP["Components\nGoalInput / Scratchpad\nToolPanel / MemoryPanel"]
    end

    XW --> LLM
    DX --> DA
    DA --> SIG
    DA --> MOD
    MOD --> LLM
    LLM --> TOOLS
    LLM --> MEM
    LLM --> MSG
    LLM --> SCH
    LLM --> MCP
    MEM --> LDB
    LLM --> SES
    TOOLS --> WS
    RT --> XW
    APP --> HOOK
    HOOK --> SVC
    SVC -.->|"HTTP POST\n/api/chat"| XW
    APP --> COMP
```

---

## 五、核心業務流時序圖

### 5.1 ReAct 工具循環（主流程）

```mermaid
%%{init: {
  'theme': 'dark',
  'themeVariables': {
    'primaryColor': '#464040f8',
    'primaryTextColor': '#ffffff',
    'primaryBorderColor': '#ffffff',
    'lineColor': '#ffffff',
    'secondaryColor': '#3a3a3a',
    'tertiaryColor': '#2a2a2a',
    'noteBkgColor': '#2a2a2a',
    'noteTextColor': '#ffffff',
    'actorBkg': '#2a2a2a',
    'actorTextColor': '#ffffff',
    'actorBorder': '#aaaaaa',
    'messageColor': '#ffffff',
    'messageTextColor': '#ffffff',
    'labelTextColor': '#ffffff',
    'loopTextColor': '#ffffff',
    'textColor': '#ffffff',
    'activationBkgColor': '#464040f8',
    'sequenceNumberColor': '#ffffff',
    'fontSize': '15px',
    'fontFamily': 'ui-sans-serif, system-ui, sans-serif'
  }
}}%%
sequenceDiagram
    autonumber
    actor User as 用戶（微信/Web）
    participant Server as Python HTTP Server<br/>xiaowang.py
    participant LLM_mod as llm.py<br/>工具循環
    participant Mem as memory.py<br/>記憶管線
    participant Tools as tools.py<br/>工具登錄
    participant TLLM as Tencent Hunyuan<br/>glm-5

    User->>Server: 發送訊息
    Server->>Mem: retrieve_memories(message)
    Mem-->>Server: top-5 相關記憶

    Server->>LLM_mod: chat(session_key, message, memories)
    LLM_mod->>TLLM: API 請求（含系統提示 + 記憶）

    loop ReAct 工具決策循環
        TLLM-->>LLM_mod: 返回 tool_calls
        LLM_mod->>Tools: execute_tool(name, args)
        Tools-->>LLM_mod: tool_result
        LLM_mod->>TLLM: 追加 tool_result，繼續推理
    end

    TLLM-->>LLM_mod: 最終文字回應（無 tool_calls）
    LLM_mod-->>Server: response_text

    Server->>Mem: compress_and_store(conversation)
    Note over Mem: LLM 提取事實 → 向量化 → 去重 → 存入 LanceDB

    Server->>User: 分段發送回應（1800 字/塊）
```

### 5.2 記憶管線（三階段）

```mermaid
%%{init: {
  'theme': 'dark',
  'themeVariables': {
    'primaryColor': '#464040f8',
    'primaryTextColor': '#ffffff',
    'primaryBorderColor': '#ffffff',
    'lineColor': '#ffffff',
    'secondaryColor': '#3a3a3a',
    'tertiaryColor': '#2a2a2a',
    'noteBkgColor': '#2a2a2a',
    'noteTextColor': '#ffffff',
    'actorBkg': '#2a2a2a',
    'actorTextColor': '#ffffff',
    'actorBorder': '#aaaaaa',
    'messageColor': '#ffffff',
    'messageTextColor': '#ffffff',
    'textColor': '#ffffff',
    'activationBkgColor': '#464040f8',
    'sequenceNumberColor': '#ffffff',
    'fontSize': '15px',
    'fontFamily': 'ui-sans-serif, system-ui, sans-serif'
  }
}}%%
sequenceDiagram
    autonumber
    participant Caller as llm.py
    participant Compress as memory.py<br/>Stage 1: Compress
    participant Dedup as memory.py<br/>Stage 2: Deduplicate
    participant Store as LanceDB<br/>向量存儲
    participant Gemini as Google Gemini<br/>Embedding API

    Caller->>Compress: compress(conversation_history)
    Compress->>Gemini: 呼叫 LLM 提取：人物/事件/時間/關鍵詞
    Gemini-->>Compress: structured memory object

    Compress->>Gemini: embed(memory_text)
    Gemini-->>Compress: vector[3072]

    Compress->>Dedup: deduplicate(new_memory, vector)
    Dedup->>Store: search(vector, top_k=10)
    Store-->>Dedup: existing_memories

    alt cosine_similarity > 0.92
        Dedup-->>Caller: 跳過（重複）
    else 新記憶
        Dedup->>Store: insert(memory + vector)
        Store-->>Caller: 儲存成功
    end
```

---

## 六、風險清單（P0 / P1）

### P0 — 必須立即處理

| # | 問題 | 位置 | 影響 | 修復建議 |
|---|------|------|------|----------|
| P0-1 | **明文 API Key** | `config.json` | 憑證洩漏 | 改用環境變數 + `.env.example` |
| P0-2 | **exec 工具無沙箱** | `tools.py` 的 `exec` 工具 | 任意代碼執行 | 加 `subprocess` 白名單 + 超時強制 |
| P0-3 | **Thread 無上限** | `xiaowang.py` ThreadingMixIn | OOM 風險 | 改用 `ThreadPoolExecutor(max_workers=N)` |

### P1 — 優先規劃

| # | 問題 | 位置 | 影響 | 修復建議 |
|---|------|------|------|----------|
| P1-1 | **God Class** | `dspy_agent/__init__.py` 609 行 | 難以測試/維護 | 拆分 Orchestrator / State / Runner |
| P1-2 | **Sessions 明文 JSON** | `sessions/*.json` | 對話歷史洩漏 | 加密存儲或用 Redis |
| P1-3 | **無整合測試** | 全專案 | 重構風險 | 以 `pytest` + 真實 LLM Mock 建立回歸測試 |
| P1-4 | **固定相似度閾值** | `memory.py` 0.92 | 記憶遺漏/重複 | 改為可配置，依 session 長度動態調整 |

---

## 七、改進建議（含代碼範例）

### 7.1 P0-1：API Key 改用環境變數

**Before** (`config.json`):
```json
{
  "models.providers.glm-5": {
    "api_key": "sk-abc123..."
  }
}
```

**After** (`config.py`):
```python
import os
from dataclasses import dataclass

@dataclass
class LLMConfig:
    api_key: str = os.environ["TENCENT_API_KEY"]
    api_base: str = os.environ.get("TENCENT_API_BASE", "https://api.lkeap.cloud.tencent.com/coding/v3")
    max_tokens: int = 16384
    timeout: int = 120
```

```bash
# .env.example
TENCENT_API_KEY=your_tencent_key_here
GEMINI_API_KEY=your_gemini_key_here
WECHAT_CORP_SECRET=your_wechat_secret_here
```

---

### 7.2 P0-3：限制線程數

**Before** (`xiaowang.py`):
```python
class ThreadedHTTPServer(ThreadingMixIn, HTTPServer):
    pass
```

**After**:
```python
from concurrent.futures import ThreadPoolExecutor

class BoundedThreadedHTTPServer(HTTPServer):
    """限制最大並發線程數，防止 OOM"""
    def __init__(self, *args, max_workers: int = 20, **kwargs):
        super().__init__(*args, **kwargs)
        self._executor = ThreadPoolExecutor(max_workers=max_workers)

    def process_request(self, request, client_address):
        self._executor.submit(self.process_request_thread, request, client_address)
```

---

### 7.3 P1-1：拆分 dspy_agent God Class

**建議拆分結構**（`dspy_agent/` 目錄下）:
```python
# orchestrator.py — 只負責任務分解與步驟調度
class AgentOrchestrator:
    def __init__(self, tool_registry: ToolRegistry, memory: MemoryModule):
        self._tools = tool_registry
        self._memory = memory

    def run(self, goal: str, session_id: str) -> AgentResult:
        steps = self._decompose(goal)
        return self._execute_steps(steps, session_id)

# state.py — 只負責狀態管理
@dataclass
class AgentState:
    session_id: str
    history: list[Message]
    current_step: int = 0
    is_done: bool = False

# runner.py — 只負責單步工具執行
class StepRunner:
    def execute(self, step: Step, state: AgentState) -> StepResult: ...
```

---

## 八、技術棧摘要

| 層次 | 技術 | 版本 | 用途 |
| :--- | :--- | :--- | :--- |
| **Frontend** | React | 19.2.4 | UI 儀表板 |
| **Frontend** | TypeScript | 5.9 | 型別安全 |
| **Frontend** | Rsbuild | 1.3.8 | Rust-based 打包 |
| **Frontend** | Tailwind CSS | 4.2.2 | 樣式 |
| **Backend** | Python | 3.x | 核心業務邏輯 |
| **Backend** | DSPy | latest | Agent 框架 + Teleprompter |
| **Backend** | APScheduler | latest | 排程系統 |
| **LLM** | Tencent Hunyuan glm-5 | - | 主推理模型 |
| **Embedding** | Google Gemini | gemini-embedding-001 | 記憶向量化（3072 維） |
| **Storage** | LanceDB | latest | 向量記憶持久化 |
| **Messaging** | WeChat Work API | - | 企業微信整合 |

---

## 九、自我審計結果

- [x] **Check 1**: 已分析全專案根目錄含全部子目錄（`dspy_agent/`, `frontend/src/`, `services/`）
- [x] **Check 2**: 改進建議含具體代碼範例（7.1 環境變數、7.2 線程池、7.3 拆分 God Class）
- [x] **Check 3**: 所有 Mermaid 標籤使用雙引號，無特殊字符衝突
- [x] **Check 4**: 使用暗色主題 + 白色文字，確保高對比度可讀性

---

*報告由 Claude Sonnet 4.6 自動生成，基於靜態代碼分析。所有觀察均附有文件路徑佐證。*
